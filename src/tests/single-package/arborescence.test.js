//--------------------------------------------------------
//-- Single package - Arborescence tests
//--------------------------------------------------------
import arborescence from '../../helpers/arborescence';
import paths        from '../../helpers/paths';


arborescence.validate({ root: paths.project.root });
